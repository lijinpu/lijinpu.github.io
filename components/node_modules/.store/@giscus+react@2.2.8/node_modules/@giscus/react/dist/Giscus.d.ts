/// <reference types="react" />
import type { GiscusProps } from './types';
export default function Giscus({ id, host, repo, repoId, category, categoryId, mapping, term, strict, reactionsEnabled, emitMetadata, inputPosition, theme, lang, loading, }: GiscusProps): JSX.Element | null;

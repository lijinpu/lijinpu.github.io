{
  "$schema": "https://turbo.build/schema.json",
  "packages": [ // <-- 确保有这个字段
    "."           // <-- 确保有这一行
  ],
  "pipeline": { // <-- 确保有这个字段
    "build": {
      "outputs": [".next/**"]
    },
    "lint": {}
  },
  "globalPassThroughEnv": [ // <-- 确保有这个字段
    "COREPACK_HOME"
  ]
}